plugins {
    id "com.android.application"
    id "kotlin-android"

    // ✅ Flutter Gradle Plugin debe ir al final
    id "dev.flutter.flutter-gradle-plugin"

    // 🔥 Google Services (Firebase)
    id "com.google.gms.google-services"
}

android {
    // ✅ El namespace debe coincidir con tu Application ID
    namespace "com.aminaec.amina_ec"

    compileSdk = 36
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
        coreLibraryDesugaringEnabled true
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId "com.aminaec.amina_ec"
        minSdkVersion = flutter.minSdkVersion
        targetSdk = 36

        versionCode flutter.versionCode
        versionName flutter.versionName
    }

    // ✅ Configuración de firmas
    signingConfigs {
        release {
            keyAlias 'aminaec_key'  // Tu alias
            keyPassword 'amina.2025'  // Contraseña de la clave
            storeFile file('/Users/kenethescobar/key_aminaec.jks')  // Ruta completa del keystore
            storePassword 'amina.2025'  // Contraseña del keystore
        }
    }

    buildTypes {
        release {
            // ✅ Firmar con release key
            signingConfig signingConfigs.release
            minifyEnabled true           // activa ofuscación
            shrinkResources true         // opcional, reduce tamaño
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            signingConfig signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.9.24"
    coreLibraryDesugaring "com.android.tools:desugar_jdk_libs:2.1.5"
}
